# Minimal Windows controller dependencies for the recommended setup:
# local browser UI plus remote ASR and remote embeddings.
#
# The full historical environment is still captured in requirements.txt, but
# this file is the better first install target for a clean controller machine.
textual>=8.2,<9
av>=11.0.0
emoji>=2.15.0
httpx>=0.27,<1
nltk>=3.9.4
numpy>=2,<3
onnxruntime>=1.17
soundfile>=0.12.1
yt-dlp>=2024.3.10
